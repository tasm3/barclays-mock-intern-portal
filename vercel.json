{
  "buildCommand": "cd intern-portal && npm install && npm run build",
  "outputDirectory": "intern-portal/dist",
  "installCommand": "cd intern-portal && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

